# 2026-08-24 — qwen3.6 (gpu1) restart: 262k ctx + q8_0 KV

## What was done
Brought the qwen3.6-35b-a3b llama.cpp seat (coder-ask backend) back from a user-aborted
mid-restart with two upgrades in one launch: context 196608 → 262144 and KV cache
q8_0 quantization (K and V).

## Key facts (settled)
- **Config edits** applied to `/data/agentic_trading/config/opencode/opencode.jsonc`
  (the symlink target — never `sed -i` on `~/.config/opencode/opencode.jsonc`):
  - line 166: coder-ask `"context": 196608` → `262144`
  - line 945: qwen-coder `"steps": 64` → `200`
  - Brace balance 119/119 verified; symlink intact.
  - Backups in `/var/tmp/opencode.jsonc.bak.*`.
- **Server**: `scripts/launch_qwen36_35b_q8.sh --ctx 262144 --kv q8_0 --port 8081`,
  `CUDA_VISIBLE_DEVICES=1`. Live props: `n_ctx: 262144`, `total_slots: 4`,
  `--cache-type-k q8_0 --cache-type-v q8_0`.
- **VRAM**: gpu1 at 45,695 used / 51,556 free — below the pre-restart 46,705, consistent
  with q8_0 halving KV. Launcher's VRAM guard passed (need ~39,460).
- **Log**: `/data/agentic_trading/logs/qwen36_gpu1_262k_q8.log`.

## Failure mode worth remembering
- First relaunch used `nohup ... &` inside the shell tool; the model loaded fine
  (log showed `n_ctx = 262144` and `server is listening`) but the tool's 120s timeout
  killed the whole process group, taking the server down (`cleaning up before exit`).
  Fix: **`setsid` + `</dev/null` + `&`** — the process survives the shell tool timeout.
  Use this pattern for any long-running launch from a tool call.
- The original restart was aborted because the user's instruction to qwen3.6 hit qwen3.6's
  own seat (it was executing the kill of its own server). The follow-up relaunch was done
  from the coder-ask seat, which is safe.

## Follow-up: mml trap re-armed and synced (same day)
- Board check for uncommitted work found `scripts/llm_swap.py` (pending 8/23 fix) had
  `mml=196608` for qwen3.6 — matched the OLD resident ctx. After today's relaunch at
  262144, any `llm-model qwen3.6-35b-a3b-q8` re-assert would hit the ctx-mismatch branch
  and `stop_all()` — evicting ALL model servers incl. the :8011 validator lane. The exact
  trap the pending diff's own comment documents from 2026-08-23, re-armed one level up
  because the 8/23 fix was uncommitted when we relaunched.
- Fix: `mml=196608` → `262144` + comment appended, committed `1fd207d1` (includes the 8/23
  30B dead-no-role tombstone that rode along in the same pending diff).
- Committed today: `9e34e462` (opencode.jsonc, 4 config changes), `1fd207d1` (llm_swap).
  `daef231d` (validator lane) was committed by the other seat.
- Remaining STALE-DOC references (committed, not working-tree — flagged, not committed):
  `launch_qwen36_35b_q8.sh:72` `DEF_CTX=196608` and `.ai/tune_registry.yaml:377`
  `context: 98304` for qwen3.6. Neither is on a code path (DEF_CTX only applies to bare
  `llm-alloc add`; registry is a record) but both disagree with the live 262144.

## Next
- Step 3 (restart `opencode serve` so the cached per-directory config picks up the edits)
  DONE 2026-08-24 ~15:35 via `opencode-restart -y` (earlier bare runs refused: attached TUI
  `1637019` on `/var/tmp/oc_sqlmesh_builder`; `-y` drops it). Verified after: :4096 serving
  (401 unauth probe = up), :8081 still 200 with n_ctx 262144. Config edits now live.
  Note: the other session's edits to the same file (reasoningEffort medium, temperature 0.8)
  took effect in the same restart.

## Full action plan (verbatim, as issued)

```
The commands. Run them in order.

  1. Config edits — edit the real path, not the symlink

  ~/.config/opencode/opencode.jsonc is a symlink; sed -i would replace it with a regular file and break the link. Edit the target directly:

  cd /data/agentic_trading
  cp config/opencode/opencode.jsonc /var/tmp/opencode.jsonc.bak.$(date -u +%Y%m%dT%H%M%SZ)

  sed -i '166s/"context": 196608/"context": 262144/' config/opencode/opencode.jsonc
  sed -i '945s/"steps": 64,/"steps": 200,/'         config/opencode/opencode.jsonc

  Verify — both must print, and the symlink must survive:

  sed -n '166p;945p' config/opencode/opencode.jsonc
  ls -la ~/.config/opencode/opencode.jsonc | grep -q '^l' && echo "symlink intact" || echo "SYMLINK BROKEN - restore from backup"
  python3 -c "t=open('/data/agentic_trading/config/opencode/opencode.jsonc').read(); print('braces', t.count('{'), t.count('}'))"

  Line 166 is coder-ask (unique in the file); line 945 is inside qwen-coder, whose block opens at 939.

  2 + 5. Restart the coder — one restart does both

  It's on gpu1 with CUDA_VISIBLE_DEVICES=1, currently -c 196608 and no --cache-type flags.

  kill -TERM 402909
  sleep 5; pgrep -f llama-server || echo "down"

  CUDA_VISIBLE_DEVICES=1 nohup bash scripts/launch_qwen36_35b_q8.sh \
    --ctx 262144 --kv q8_0 --port 8081 \
    > /data/agentic_trading/logs/qwen36_gpu1_262k_q8.log 2>&1 &

  The launcher's own guard checks gpu1 free VRAM against its computed floor and refuses if short — gpu1 has 50,546 free against a 39,460 need, so it should
  pass.

  Verify:

  curl -s http://127.0.0.1:8081/props | python3 -m json.tool | grep -E "n_ctx|total_slots"
  nvidia-smi --query-gpu=index,memory.used,memory.free --format=csv,noheader | sed -n 2p

  Expect n_ctx: 262144 and gpu1 around 46,040 used — lower than the 46,705 it sits at now, because q8_0 halves KV.

  3. Restart opencode serve

  The config is cached per-directory until the serve process restarts, so none of the edits take effect until then.

  Rollback

  cp /var/tmp/opencode.jsonc.bak.<stamp> /data/agentic_trading/config/opencode/opencode.jsonc
  CUDA_VISIBLE_DEVICES=1 nohup bash scripts/launch_qwen36_35b_q8.sh --ctx 196608 --port 8081 > /tmp/rb.log 2>&1 &

  One thing to watch after: q8_0 KV is measured at <0.1% perplexity delta generally, but no study I found covers hybrid deltanet MoE. If delegate quality
  drops, that flag is the first suspect and the rollback above is a one-flag restart.
```

## Execution notes vs plan
- Steps 1 and 2+5 executed and verified (see "Key facts" above). Step 2's `kill -TERM 402909`
  was aborted by the user (it hit qwen3.6's own seat); the server ended down anyway and was
  relaunched by this seat with the `setsid` fix.
- Deviation: used `setsid bash ... > log 2>&1 < /dev/null &` instead of `nohup ... &` —
  see failure mode above.
- Rollback recipe preserved verbatim above (config backup restore + one-flag restart).
- Watch item preserved: q8_0 KV unmeasured on hybrid deltanet MoE; first suspect if
  delegate quality drops; rollback is a one-flag restart.
