[Service]
# Kill the entire cgroup (not just main process) when memory is exceeded.
# This prevents orphaned subagent processes from consuming host memory.
# The 8G MemoryMax + OOMPolicy=kill gives a hard boundary for the server
# + any subagents running inside it.
OOMPolicy=kill
