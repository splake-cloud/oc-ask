# opencode_memory — session memory index

Conventions (what "write a session receipt" / "reorient me" mean): see `README.md`
and `/home/user/oc-ask/AGENTS.md` (auto-loaded every session).

One line per memory card, newest first. "Reorient me" → read this index, open matching card.

Memory repo: `git -C /home/user/oc-ask` → `git -C /home/user/oc-ask add opencode_memory/ && git -C /home/user/oc-ask commit -m "memory" && git -C /home/user/oc-ask push`
Remote: `https://github.com/splake-cloud/oc-ask` (branch `master`, commit `c17a5bf4`)

| Card | Topic | Key contents |
|---|---|---|
| [2026-08-31_iron_fly_l4b_R_x_pw_nogo.md](2026-08-31_iron_fly_l4b_R_x_pw_nogo.md) | Iron Fly **L4-B R×P_W interaction**: CLOSED **NO GO** (PM 2026-08-31) | Session `ses_fabec1502ffeehxAP14Y11aDp4`. PM asked for a build spec for R×P_W **if the L4-A secondary grid showed stable structure worth promoting**. Re-examined the actual L4-A secondary grid (single median P_W split per R band, gated-290) BEFORE drafting — conditional did NOT fire. **Only candidate B1×low-P_W** (mean −2.54, 8/17 negative) vs B1 high-P_W (+14.23): n=17/9 wks, week-cluster CI **[−11.47, +38.53] crosses zero**. No monotone direction across bands (B1 low negative, B2/B3 low positive); B3's gap = 2-trade/1-wk artifact (L4-A OQ3 tiny-cell ban); 2 cells ≠ gradient. **PM: NO GO — no 2-D build authorized; reconsider only with materially more data/independent evidence.** Blueprint `specs/iron_fly_L4-B_R_x_pw_interaction_scope.md` (CLOSED; §1.1 decision block, §1.2 evidence, §3–§11 the design that would have resolved it, fixed P_W boundaries 0.50/0.70/0.90 NOT iterated — now a decision record). Defect caught in my draft: L2-F SHA256 truncated to 52 chars (would break G0); fixed + verified 64-char (verify-run `l4b_spec_hash_check` HASH_MATCH). Uncommitted. **NEXT PRIORITY (PM): entry-known realized movement / range state within the frozen R-qualified pop** — two trades both R-acceptable, but one entered while SPX already traversing unusually large space vs the 100-pt structure → may reduce TP50 completion even when premium looks attractive. Genuinely untested interaction with current completion decay. Premium-quality refinements EXHAUSTED: R level (L4-A MIXED/no slope), R×P_W (L4-B NO GO), FOMC (L5-A real env diff, uncertain rule value). Not yet scoped. |
| [2026-08-31_iron_fly_l5a_fomc_scoped.md](2026-08-31_iron_fly_l5a_fomc_scoped.md) | Iron Fly **L5-A FOMC event conditioning**: BUILT + CLOSED | Session `ses_fabec1502ffeehxAP14Y11aDp4`. Does a FOMC statement landing INSIDE the Mon11:30→Fri15:59 hold window change the iron-fly outcome? **CLOSED 2026-08-31. RESULT = MIXED: FOMC weeks are a DIFFERENT trade (higher premium: credit +3.8, friday-straddle +10.7, R −0.053; larger realized move: terminal +13.1 pts, max excursion +15.7 pts; more wing breaches: terminal +0.118, any-time +0.095); net mean TP50 PnL 4.90 vs 14.97 (Δ −10.06, week-cluster 95% CI [−23.44, +3.50] CROSSES ZERO). Same direction gated + both grids (grid-5 CI excludes zero). Table 3 (n=32): no consistent same-day direction. Sensitivity excl-2025-08-22: robust. No rule change. Receipt `receipts/l5a_fomc_event_conditioning.md`.** **Split CORRECTED: 33 FOMC / 146 non-FOMC on the 179-week frozen CSV (NOT the review's 34/145 which was the 182-week warehouse); 2 in-window meetings (2025-03-19, 2026-06-17) have no enterable 100W trade → excluded. G1 gate now pins exactly 33/146 + exact 33-date set.** **Data-quality flags: MFE/MDD are PnL-path metrics not SPX distance (the "lower FOMC MDD" = two 2024-04-22 non-FOMC outliers mdd −4996.67; median MDD identical); `realized_move_terminal_pct_wing` mislabeled (spec (pts/100)*100 no-op = points); spot_1min lacks 16:00 bar for 2026-04-29+2026-07-29 (NA, incomplete_window=1); tp50_win_rate==tp50_reach by construction (all reached trades pnl>0).** Build qwen-coder `ses_fa7b0f14cffezMzHQvmDhNxJCq` + G1-EDIT `ses_fa79a9dfdffedufKkPyzn018ho`; verify-run `l5a_final_gate` (all G0–G5 PASS, byte-identical, 0 FAIL). All artifacts uncommitted. |
| [2026-08-31_iron_fly_l4a_within_gate_R_level.md](2026-08-31_iron_fly_l4a_within_gate_R_level.md) | Iron Fly **L4-A within-gate R level** (cliff vs slope): scoped, **RATIFIED v2**, built, verified — **NOT committed** | Session `ses_fabec1502ffeehxAP14Y11aDp4`. Within R≥0.70 (n=290) / R≥0.80 (n=184), does R LEVEL carry TP50 info beyond the gate? PM rulings OQ1–OQ5 (fixed bands B1-B4 106/108/68/8 + quantiles; seed 20260901; P_W interaction SECONDARY single median split; friday_hold_pnl = terminal comparator only; small_sample C(T)<10). §4.0 interpretation contract (CLIFF/PLATEAU / SLOPE / MIXED, deterministic procedure, core = reach/win/meanTP50/35→50). **1 bounce:** build used P(pnl>0) not spec's "reach AND pnl>0" for win rate (0.6690 vs 0.5897; in this data reach⇒pnl>0 so spec==reach rate); 2-line EDIT fixed. verify-run v1-v4 + fix v1-v4 all PASS, BYTE_IDENTICAL, independent recompute exact (anchors 13.9987/13.9641, all band means, Spearman, 3 CI cells, agg win50 0.589655). **Result: MIXED both pops** — no cliff, no slope: reach/win irregular (dip B3, B4 n=8 spike), continuation cliff-like only, quantile ordering FALSE, Spearman(R,tp50)≈−0.03. Answer: 0.70 gate is NOT the foot of a monotone slope. **CLOSED & committed `7565b546`** (10 files; PM banked MIXED verbatim; continuation = only cliff-like shape; receipt `receipts/l4a_within_gate_R_level.md`). |
| [2026-08-31_sqlmesh_advance_reenable_and_iron_fly_hold.md](2026-08-31_sqlmesh_advance_reenable_and_iron_fly_hold.md) | SQLMesh warehouse advance **re-enabled** (K=3 in progress) + iron-fly field-contract **HOLD** | PM: remove `--skip-shadow`, verify 5 criteria/run, K=3 clean scheduled runs; iron-fly scheduling metadata unchanged unless cadence fails. Staged unit (root-owned, PM applied; confirmed no --skip-shadow). **Controlled manual de-risk run** trade_date 08-30, 157.3s all_ok, step10 `warehouse_shadow ok 0 unexplained`, **5/5 PASS** (c1 no regress, c2 IV M1+M2 08-27→08-28 FULL M2 kept pace, c3 no false-complete, c4 janitor 34/34, c5 ledger 08-31 zero unexplained). Harness c3 bug fixed (Sunday false-positive). Code facts: step10 `shadow_run.py daily`→`ctx.run()` on LIVE db; `ctx.run()` runs janitor INTERNALLY (context.py:804-805); standalone janitor skipped (no double-run); reconcile=19 daily tables (iron_fly/IV weekly NOT in registry). IV correction: IV weekly reads OPTIONS POOL not stale vix_daily. **Health check HEALTHY 10 PASS/1 WARN/0 FAIL** (`verify/sqlmesh_health_20260831/`). The WARN = unapplied semantic change in `iron_fly_weekly_substrate_v2.sql` (fly_value_mid capped [−50,50] 66.3% rows; entry_quoteable→TRUE flips 375,936; load-bearing: L-studies filter population on entry_quoteable + use fly_value_mid for extreme-mark diag). **PM Option 3: HOLD — do NOT apply/discard**; live snapshot 1103730983 authoritative, K=3 unaffected; safer shape = keep uncapped raw + separate bounded + OOB flag + genuine quoteable; do NOT plan-apply working tree until adjudicated. Note `studies/iron_fly_weekly/receipts/iron_fly_v2_field_contract_HOLD.md`. Next: K=3 run1 tonight 23:30 UTC. |
| [2026-08-31_sqlmesh_stage23_closeout.md](2026-08-31_sqlmesh_stage23_closeout.md) | SQLMesh Stage 2+3 iron-fly recovery: **formally CLOSED** (resumed after ctx-overflow) | Full-week restate `[2026-08-24,2026-08-29)` on `iron_fly_weekly_substrate_v2` (v1103730983) was ALREADY applied rc=0 + P1–P7 PASS before overflow; overflow truncated the closeout JSON + left scratch clone. Re-verified live read-only (sha `80a489f5…`, WAL absent, 1,949,021 rows/901 d/max 08-28/iv 08-29/5 dates 1620-2340-2340-2340-2100/dup 0/only-iron-changed/prod-refs-unchanged). **Regenerated `08_closeout_proofs.json`** fail-closed (temp+JSON-validate+atomic-rename; preserved truncated as `.truncated`; 12 values reconciled vs `07`+live, ALL_CLOSEOUT_OK). **Created+hash-verified post-restatement snapshot** `…bak-post-restatement-stage23-20260831T034447Z` (sha==live). **Marked failed-apply backup** `.DO_NOT_RESTORE` (PM wording, NO "version None"). **Removed 2 scratch targets** (no wildcards, parent intact, 28 entries kept) = **53.70 GB reclaimed**. 4 backups kept (pre-migration-02360, pre-apply-0.236.1, FAILED-apply, post-restatement). 21/21 final checks PASS; verify-run `stage23_closeout_*`. Dirty agentic_trading worktree (unrelated L3/iv) untouched. **CLOSED: committed `7a980379` "ops: close iron fly warehouse recovery" (22 files: evidence dir + 5 transcripts + incident receipt w/ provenance section: 2-day restate NOT valid, full-week restate [08-24,08-29) v1103730983 authoritative; DO_NOT_RESTORE sidecar stays in backup archive). PM ruled stale-warehouse incident CLOSED.** |
| [2026-08-31_iron_fly_l3a_recent_environment_strength.md](2026-08-31_iron_fly_l3a_recent_environment_strength.md) | Iron Fly **L3-A + L3-B + L3-C** environment-strength trio: all scoped (PM-ratified), built, verified, **CLOSED & committed** (L3-A `27eac92c`, L3-B `9837638e`, L3-C `a4a6da84`) | Session `ses_fabec1502ffeehxAP14Y11aDp4`. Frozen trade (100W/Mon11:30/g5g25/R≥0.70+0.80/TP50 headline), windows 6M/12M/18M/24M/36M/FULL, **exact calendar-month cutoffs** (anchor 2026-08-17 11:30), 95% deterministic **week-cluster bootstrap** CI (10k resamples, seed 20260831), descriptive only. Pure frozen-artifact study (L2-F 100W leg + L2-E join, no substrate). **2 bounces fixed:** (1) my spec's G4 anchor 13.9641 was the n=280 L2-F A1 value — L3-A estimand full-290 = **13.9987** (independently verified); build also read a 3rd CSV violating closed read list → in-memory fix; (2) build blanked the 6M-robustness CI — ratified §4.1 says compute+flag (C(T)=2), blank only C(T)<2 → now −42.73/28.01 + small_sample. Join-key format in spec corrected (`_1130_005_0100_` not `_g5_0100_`). verify-run v1–v4 on fixed build: compile 0, full run G0–G6 ALL PASS, byte-identical rerun, FAIL=0. **All 12 CI cells independently reproduced** from frozen inputs (shared seed stream, order-dependent by construction). Result: mean TP50 PnL 3.32/10.75/11.06/14.03/17.04/14.00 (6M collapses, CI spans negative); R pass rate dips 18M (0.729); mean abs(MDD) jumps 36M/FULL (74.8/59.6, medians stable ~23–33). **NOT committed** — awaiting PM receipt/commit ruling. Receipt `receipts/l3a_recent_environment_strength.md`. **L3-B** (non-overlapping Latest 6M/12M/18M vs prior): first clean build of the series (no defects); Δ mean TP50 PnL −11.42/−6.72/−21.28, only 18M Δ CI excludes zero (prior 2024-02→2025-02 = strongest stretch ever); 12M reach/win flat-to-positive while mean negative (Friday-terminal drag); 18M Δ absMDD −126.4 = deep-drawdown artifact; all 6 paired-bootstrap CI cells independently reproduced. Receipt `receipts/l3b_recent_vs_prior_period.md`. **L3-C** (rolling 12M, 39 month-ends 2023-06→2026-08 + TP50 payoff decomposition): first clean build; rolling mean TP50 PnL CYCLES (trough 2024-03=1.18, peak 2024-10→2025-05≈30–34, decay to ~10 by 2026, 2026-08 partial=9.86). **Mechanism (banked):** collapse ~31→~10 is a reach-rate/mix shift NOT loser-severity — reacher count halves 62→33 while reacher mean RISES (→50.9) and non-reacher mean IMPROVES (−20.7→−13.7); growing non-reacher weight drives the mean. **PM ruling (verbatim):** iron fly still positive latest-12M but 2024-H2/2025-H1 edge decayed; principal deterioration = lower TP50 completion probability. First image artifacts in series (2 PNGs). Receipt `receipts/l3c_rolling_12m_strength.md`. |
| [2026-08-30_iron_fly_l2e_premium_wing_banked.md](2026-08-30_iron_fly_l2e_premium_wing_banked.md) | Iron Fly L2-E premium/wing study: built (post-reboot re-dispatch), 3 output defects fixed, banked | Session `ses_fabec1502ffeehxAP14Y11aDp4`. First build KILLED by reboot (dispatch `ses_fac0c0b82ffeqsRL44hd43XmWW`, 0 artifacts → clean restart). Re-dispatch `ses_fabe89344ffeZXqPd5bRb0Q37O` (1639-line script, G1–G6 PASS, G6 290/290 exact, deterministic). Independent review caught **3 bounded impl defects** (NOT design changes), fixed by EDIT `ses_fabd30dbbffeN7YpT1tps71G1S` (7-line diff): (1) A7 contrast used full-352 A1 bounds (18.22) not within-290 → **5.71** (Q1 n=74/Q4 n=70); (2) Spearman p unclamped 1.1062→1.0; (3) A2 report boundary label A1's→within-290 0.50/0.57/0.64. Headline recomputed, **unchanged**: MONOTONICITY(R≥0.70) **PARTIAL** 1/3, ROBUST(R≥0.80) **PARTIAL** 2/3, INCREMENTAL BEYOND R **ADDS** (survives R-band + residualization). **PM RULING (banked verbatim):** P_W **RETAINED** as material structure-quality variable for the forthcoming wing-width (75W/100W/120W) comparison — not fully monotonic (ρ(P_W,reach)≈0) but higher P_W improves TP50 economics at the extremes + incremental value after R (ρ(P_W,TP P&L)≈+0.2–0.3 sig); R stays entry conditioner, P_W not a standalone gate. L2-D headline unchanged. verify-run v1/v2/v3 byte-identical 8/8, FAIL=0. **Trap:** my own recompute gave 6.13 using rounded boundary literals; exact-float value 5.71 is correct — recompute from population not printed labels. CLOSED & committed; receipt `studies/iron_fly_weekly/receipts/l2e_premium_wing_100w.md`. |
| [2026-08-30_stale_warehouse_incident_forensics_recovery.md](2026-08-30_stale_warehouse_incident_forensics_recovery.md) | Stale-warehouse incident: 5-deliverable scope + **Stage 0 preflight EXECUTED** (2026-08-31) | PM dispatched to THIS seat (overriding "hand to SQLMesh agent"). All 5 delivered; **Stage 0 read-only preflight now EXECUTED** per PM auth (snapshot sha256 `cb5704ab…`, quiescence proven, no mutations). **D1:** 00:20:50 journal = es_live_ingest ONLY (no sqlmesh); row loss = **22:35:23 restate** (real DELETE+INSERT on `__1230283500`) — **2026-08-27 card "NO-OP" marked SUPERSEDED** (preserved+linked). Mirror root = `shadow_run` ephemeral dir, NOT rogue. **D3:** state `end_ts` EXCLUSIVE; real stale = breadth_daily + weekly_vix_study **physical 04-10 (137-138 d, FULL)**, iron_fly_v2 08-21 (5 d). **KEY Stage-0 finding: BOTH deep-stale sources are STALE at 04-10** (firrate SPX zip content max 04-10; vix_daily.parquet max 04-10) → models BLOCKED on separately-gated **source-refresh plan** (do NOT re-derive from stale inputs). **Recovery matrix:** iron_fly_v2 (daily point-in-time substrate for a weekly strategy) 08-24..08-25 = **FALSE-COMPLETE** (state claims done, bytes absent → normal advance won't revisit; needs scoped `--restate-model` of only those 2 days) + 08-26..08-28 genuine gap = 5 missing trading days; daily models = ordinary advance. **Sharpened 5-step sequence** (each separately PM-gated): keep `--skip-shadow` → scoped restate 08-24..25 → prove 2 dates + reconcile rows/keys/states → gated advance through 08-28 → prove 5-session continuity 08-24..28 + other dailies at 08-28 → only then evaluate restoring scheduled advance. **PM ruling applied:** forensic conclusion ACCEPTED; durable invocation auditing APPROVED as design task; Stage 4 janitor now CONDITIONAL on `janitor --dry-run`; **K=3** + 4 proofs (frontier/reference/physical-table/writer-attribution); `--skip-shadow`+Option B stay in place. 7 verify-run transcripts `verify/stage0_*` + `verify/stale_wh_*`. |
| [2026-08-30_optionB_standalone_janitor_implemented.md](2026-08-30_optionB_standalone_janitor_implemented.md) | Option B standalone SQLMesh janitor: implemented + verified (patch for PM review) | Resumed DEAD thread (prior agent hit ContextOverflowError before dispatching the edit). Delivered: gated non-blocking `warehouse_janitor` step in `scripts/daily_eod_build.py` (+105/-0) + `tests/test_daily_eod_janitor.py` (22 mocked tests, **22 passed** via verify-run). **Nothing live; unit untouched.** Gate `:618` = `_shadow_skipped AND _blocking_steps_ok` → runs only when `--skip-shadow` set AND all 8 blocking steps ok; if advance re-enabled, skipped (Context.run() owns it → no double-run). Non-blocking is structural: returns only ok/janitor_alert; all_ok keys on failed/shadow_alert only. Full stdout/stderr → `LOG_DIR/janitor_diag_*.json`; detail=last line. Command exactly `[.venv/bin/sqlmesh, -p, warehouse, janitor]` no flags. **Deliberate deviations from draft:** no `--skip-janitor` flag, no log renumber. Proof+execution plan: `studies/iv_weekly_substrate/receipts/2026-08-30_optionB_janitor_control_flow_proof.md`. **Separate priority incident (SQLMesh agent, NOT this seat):** stale warehouse (pool atm_iv 08-28 vs warehouse 08-25, M1/M2 08-27) + 00:20:50 mystery write + frontier-day recovery. Reassess step removal once shadow path restored. |
| [2026-08-30_iron_fly_l2d1_exclusion_accounting_fix.md](2026-08-30_iron_fly_l2d1_exclusion_accounting_fix.md) | Iron Fly L2-D1 body-convergence anatomy: exclusion-accounting defect fixed + re-run | Resumed in-flight from `ses_faf5ffcc8ffeeq4yisMKvcSql0` (prior agent had diagnosed but never re-run the fixed script — on-disk outputs predated the final edit). **Defect:** parent set (Gate 2 vs frozen build) = 119+75 = **194**, but pre-fix run reported eligible=126 (C1–C4=103+7+11+5) + Gate 4 "excluded=0" → **68 parent trades silently dropped** (selection bias PM ruling #1 forbade). Mechanism: parent rule uses `_spx_eff` (carry-back-1) but `classify_geometry` needs raw `spx[t-10]`+`spx[t-29..t]`+`spx[t-30]` non-NULL; a parent with `spx[t-30]` NULL is unclassifiable; pre-fix loop dropped it with no record, Gate 4 counted only inside `leaf_assignments` (never contained them)→always 0, Gate 3 circular. **Fix (qwen-coder, on disk, verified):** `classify_geometry` 6-tuple w/ `excluded_reason`; main loop records EVERY parent trade w/ delta; Gate 3 non-circular (`classified+excluded==parent_total`); Gate 4 per-pop counts by reason + escalation (>5%) + eligible-vs-excluded mean Δ; report gains "Selection Bias" section w/ caveat. **Re-run (verify-run `verify/l2d1_exclusion_fix_rerun_20260830T162030Z.…txt`, exit 0):** arithmetic reconciles EXACTLY — primary 74+45=119, robustness 52+23=75, total 126+68=194, leaves sum 126. Gate 3 PASS, Gate 4 REPORT w/ **ESCALATION** (37.8%/30.7%). **Bias exposed:** primary eligible Δ 4.271 vs excluded 1.230 (~3.5× worse — convergence was inflated by discarding losers); robustness 4.632 vs 4.736 (excluded slightly higher, pop-dependent). H1 now FAILS both pops, H2 FAILS, H3 mixed. **CLOSED & committed `623df92a`** "research: close L2-D1 body convergence anatomy" (script+spec+4 outputs+receipt). **Banked conclusion (PM-verbatim, `receipts/l2d1_body_convergence_anatomy.md`):** moving_toward_body remains a real raw continuation-expectancy signal but the path-anatomy decomposition is not reliable enough to explain its mechanism (37.8%/30.7% SPX-window exclusion, materially outcome-associated in primary, H1+H2 fail on accounted set) → **no anatomy-derived rule adopted, no causal/mechanistic claim, L2-D headline unchanged** (improves mean continuation P&L, fails bankable win-rate). Frozen inputs untouched (Gate 7 PASS). |
| [2026-08-30_iv_weekly_rag_cards_seeded.md](2026-08-30_iv_weekly_rag_cards_seeded.md) | IV weekly substrate RAG cards: authored, seeded, LIVE on both roots | PM asked if a RAG card describes the IV weekly data (structure+access+refresh) & if POOL_LEDGER is current. **RAG: NO** (live probe returned only the `iv_percentile_daily` donor); **POOL_LEDGER: up to date** (weekly models deliberately absent — warehouse DuckDB tables, not parquet). **Two root causes:** (1) harvester `harvest_table_cards.py` `_parse_py_model` regex `@model\s*\(\s*"` doesn't match `@model(name="...")` → M2 silently dropped; (2) M1 auto-card too thin. **Fix (EDIT, qwen-coder, verified):** regex → `(?:name\s*=\s*)?` + `SQLMESH_MODEL_OVERRIDES` dict w/ comprehensive text+payload for the 2 models only (other 32 cards byte-identical). Chose to edit the HARVESTER not hand-write staged JSONL (scan-stage would clobber). **Seeded (PM "go"):** `refresh_data_table_cards.sh stage`(268→270, +2/-0/0 content-changed)→probe→`go` re-seeded all 4 data_table wells both roots. **Verified live (Checklist E):** keyed exact-fetch FOUND both roots; `:8765` top-1 M1[8.06]/M2[8.69]. Card facts fact-checked vs live DB (3,987 rows 2021-05-13..27; M2 252d structural-NULL wk1/wk2 max-run 241/240<252). **Trap:** live RAG probe MUST pass `collections` or returns ok:false+0 (false "absent"). Harvester edit UNCOMMITTED. |
| [2026-08-30_iron_fly_l2d_decision_analysis_no_pass.md](2026-08-30_iron_fly_l2d_decision_analysis_no_pass.md) | Iron Fly L2-D decision analysis: NO marker clears the bankable bar (PM ruling executed) | **Answer: static TP35/TP45 stands** — 0 PASS / 2 MARGINAL / 5 INDETERMINATE / 105 FAIL over 112 cells (28 rules × 2 states × 2 pops). Ratified contract: Δ̄≥+0.50 (PM raised from 0.20) + headroom≥0.30, both R pops, top-5 trim ≥+0.10, **Holm across 28 rules per cell** (PM rejected prespecification-exempts-multiplicity), n_avail≥0.6× + n_continue/n_exit≥20, win-rate/capture non-inferiority −0.02, drawdown Option A (no path fetch — only valuable if a rule survives). Load-bearing design: Δ̄=(1/n_available)·Σ_{cont}Δ_i (=build's delta_vs_TP35/45; ÷n_continue reading would flip 3 cells FAIL→PASS); G4b = two-sample branch-discrimination (naive paired shuffle has identical null as G4). **Why no pass:** G6 win-rate kills the big effects (moving_toward_body A Δ̄+2.13/+3.33 but WR −4.0pp/−2.9pp; net_rising_60 A −4.9pp/−3.8pp); Holm kills the rest; MARGINAL = median_spx_disp_body B (Δ̄+0.97/+1.67, fails G4b primary Holm p=0.0896). Housekeeping: dead duplicate G4 loop removed (was corrupting RNG + spurious p_g4b≈0.0001 on degenerate selectors → now 1.0), INDETERMINATE cell-specific (median_accel_60 A/primary →FAIL), 0 warnings, 112/112 verdicts unchanged except the relabel. **Concurrency trap:** parallel seat's commit 3219a2d6 swept in a pre-fix snapshot; verified worktree-vs-HEAD, final fix = 43b8d668. Commits f7847aae/43b8d668. Scope `specs/iron_fly_L2-D_decision_analysis_scope.md` (ratified, App A/B/C). Next: nothing blocking; optional new-scope diagnostic on moving_toward_body under stricter WR floor. |
| [2026-08-30_iv_backfill_gate3_write_m2_restate_complete.md](2026-08-30_iv_backfill_gate3_write_m2_restate_complete.md) | IV backfill-to-2021 Gate-3 WRITE executed + M2 stale defect found & fixed — **COMPLETE** | **All 3 models verified:** M1 3018527636 (3,987 rows 2021-05-13..27, baseline preserved), M2 2902785128 (3,987 rows 2021-05-13..27), calendar 617669567. **Defect:** write #1 left M2 STALE (3,192 rows, 0 in 2021) — the staging adjudication **inverted `is_no_rebuild`** (`definition.py:1134`: carried-over version on INDIRECT_NON_BREAKING FULL = do NOT rebuild / reuse old table, NOT in-place re-materialization). Fixed by **M2 restate** (`--restate-model`, PM ran by hand, token `…m2_restate_…124650Z`): 3,987 rows, M1 keys missing=0, 795 rows in 2021 span; M1+calendar IDs unchanged; both KASAs PASS. **General rule:** an indirectly-modified FULL model needs an explicit restate to pick up parent data changes — plain `plan prod` won't re-run it. Data-access assessment + qwen-coder verify (8 PASS/1 PARTIAL) in `studies/iv_weekly_substrate/receipts/2026-08-30_…_assessment.md`; single full-window scan (batch_size=None); host-side run (cgroup 8GB<16GB). Traps: token hash collapses repeated --select-model (scope = models list); registry is `.sqlmesh_runtime/` not `.guarded_runtime/`. Evidence `verify/iv_backfill_gate3_20260830T001926Z/` (09-13 + transcripts). |
| [2026-08-30_iron_fly_l2d_spec_reconciled_ruling1.md](2026-08-30_iron_fly_l2d_spec_reconciled_ruling1.md) | Iron Fly L2-D continuation markers: spec reconciled to build (PM ruling 1) | **Ruling 1 executed:** in-conversation amendments operative, build `scripts/iron_fly_l2d_continuation_markers.py` UNTOUCHED (already V1–V11 11/11). **Key finding:** the "437-line conflicting v2 spec" the prior (compacted) session saw is GONE — disk now has a 457-line spec whose table header reads "authoritative — matches the implemented, verified build"; all 4 flagged conflicts (uniform >=median, band-run approaches, slope_k6_C/straddle_ratio/touch_dow, stale k6=6) were ALREADY corrected before resume. Build contract extracted via qwen-coder EXPLORE + cross-checked: 25 markers, HIGH/LOW/DESC median (14 hybrid = 6 HIGH + 8 LOW, 11 DESC), 14 natural rules, approaches/rejections = up/down-crossings of L=(A−10)%·C (rows <t), lookback 30/60, SPX carry-back ≤1 snapshot. One edit (delegated qwen-coder, verified by read-back): stale closing line 456 "Dispatch is held…" → "table approved (PM ruling 1, 2026-08-30)"; file now 459 lines, line 4 `Revision: v2` untouched. Frozen facts re-confirmed (290/184, 206/132, 183/120, 13.515/13.999). **Next = analysis/interpretation** of marker-separation + hybrid results (the study's real answer), NOT further spec work. |
| [2026-08-30_iv_backfill_gate3_staging_m2_adjudicated.md](2026-08-30_iv_backfill_gate3_staging_m2_adjudicated.md) | IV backfill-to-2021: Gate-3 staging packet built; M2 discrepancy ADJUDICATED (STOP → PASS) | **Packet PASS** `/data/agentic_trading/verify/iv_backfill_gate3_20260830T001926Z/` (8 artifacts; 3.8 resumed packet-build session, scratch `/var/tmp/ivw_gate3_staging/`). **M2 adjudicated:** measured plan = version FIELD **2902785128 carried over / INDIRECT_NON_BREAKING** (in-place re-materialization), NOT spec §2.4's 3013756842/FORWARD_ONLY — spec was wrong (used `to_version()`, the fingerprint value; FORWARD_ONLY is deprecated, asserted `definition.py:1129`). **Data NOT stale:** M2 FULL model reads whole M1 no date filter (`iv_weekly_percentile.py:109,132-135`) + in plan backfill set 2020-08-03→frontier → full re-derivation over new M1. Carry-over branch = `categorize_as` `definition.py:1134-1144` + indirect path `builder.py:752-755`; parent cascade reproduced exactly `hash_data(["3018527636"])=1695153442`. Receipt-reviewed by this seat against raw `plan_result.json` + installed sqlmesh 0.236.1 (transcript `verify/gate3_m2_adjudication_evidence.20260830T030613Z.txt`). Transitions: cal 2127916289→617669567, M1 4225993480→3018527636 (backfill from 2021-05-13), M2 carried; restatements=[], exactly 3 models. Prod sha256 invariant 386140b3… BEFORE==AFTER. **D9 closed:** `post_write_verify.py:366-421` idempotency bounded to ≤ baseline_max (2026-08-27), frontier rows reported not failed. Traps: scratch copy MUST be named `warehouse.duckdb` (catalog=stem; 3rd occurrence of the rule); `verify-run` takes `prog args`, not `python3 -c`. **Next = PM-token-gated prod write** (single scoped plan, then post_write_verify --expect-changed {cal,M2} --row-baseline, final KASA via pytest/explicit call). |
| [2026-08-29_iron_fly_l2b_freeze_l2c_management.md](2026-08-29_iron_fly_l2b_freeze_l2c_management.md) | Iron Fly L2-B frozen (Option B) + L2-C management-policy study built & verified | **L2-B FROZEN** `receipts/l2b_freeze.md` (PM verbatim banked conclusion): R = continuation-quality selector, not acquisition; gate R>=0.70 (n=290), robustness R>=0.80 (n=184), descriptive R>=0.90; **no positive-Friday-hold claim**. **Mark correction:** frozen `friday_pnl` = Monday 15:59 mark (verified 44/44, corr(Mon,Fri)=0.086); true Friday hold P&L NEGATIVE: R>=0.70 -0.52, R>=0.80 -0.77. Field rename `mean_Friday_PnL`→legacy Monday-1559 recorded in 3 places, CSVs untouched. **L2-C** `specs/iron_fly_L2-C_build_spec.md` + `scripts/iron_fly_l2c_management_policy.py` (qwen-coder `ses_fb1da0f59ffenqIEkQLffGtmth`), 16 prespecified policies, V1-V11 PASS (transcript `verify/l2c-build-full.20260829T153552Z.txt`); independent cross-checks: TP exit_rate == frozen touch rates exact, HOLD == probe. **Result: TP_50 dominates** (+13.999 vs HOLD -0.52, Δ+14.52, capture 0.408); TP_35/45 ≈ same; GB/TRAIL/TIME do not compete. Prespecified-grid report, not optimization. No commit (tree untracked). |
| [2026-08-29_iv_backfill_2021_gate1_applied.md](2026-08-29_iv_backfill_2021_gate1_applied.md) | IV backfill-to-2021: Gate-1 (C1–C8) applied + verified; Gate-2 KASA PASS | **Gate-1 DONE** (qwen-coder 3.6, `ses_fb1df2f48ffeyc7zPI1klEnI1t`): C1–C8 = ONLY diffs, frozen KAs untouched. **coder3.8 second-opinion review PASS-WITH-FIXES** (`ses_fb1ca0919ffetWV3NgMqLpC3cN`): F1 carrier file must be named `warehouse.duckdb` (catalog=filename stem) — spec §4 fixed. **Gate-2 DONE** (carrier dispatch `ses_fb046f9b5ffe5T3e2qUkqIHRU2`): carrier `/tmp/opencode/ivw_gate2_carrier/warehouse.duckdb` = materialized TABLE (F2: NOT a view — M1 body's session-scoped TEMP pool table breaks views) 3,990 rows/1,330 days min 2021-05-13 max 2026-08-28; **M2 KASA vs carrier PASS** (sqlmesh-context, `IVW_M1_DB`), M1 KASA regression guard PASS, idempotency carrier≥2022-06-01 vs prod PASS (3,192 set-equal). 8 verify-run transcripts `verify/gate1_backfill_*` + `verify/gate2_*`. ⚠ M1 KASA test file has NO `__main__` — `python3 <file>` is a no-op exit 0; must call `test_iv_weekly_state_known_answers()`. **Next = Gate-3**: capture row-baseline parquet + metadata baseline BEFORE single scoped `plan prod` [nyse_calendar, M1, M2]; `post_write_verify.py --expect-changed ... --row-baseline ...`; final KASA default path. Spec `studies/iv_weekly_substrate/specs/build_spec_iv_backfill_2021.md`. |
| [2026-08-29_iv_substrate_reorg_and_rag_card_retrievability.md](2026-08-29_iv_substrate_reorg_and_rag_card_retrievability.md) | IV substrate reorg into `studies/iv_weekly_substrate/` + RAG placement card made retrievable | **PM call: own study folder** (not under iron_fly_weekly). 9 files moved (4 specs from `.ai/inbox/`, 5 dispatch receipts from `.ai/packets/iv_weekly_percentile/` — dir removed); models+tests NOT moved (canonical shared dirs per the card). New `verify/navigation.md` = single entry point (core idea + roadmap to every artifact, incl. repo-root `verify/` transcripts). **Card fixed + re-indexed:** `study_infra` well needed `--force` (fingerprint hashes builder source only, NOT the card); small on GPU (6s), full on `--device cpu` (PM call, 8 cards → 11s); no service restart (reseed drops reader cache). **Retrieval gap closed:** `file system cleanup process` / `how much file refactoring to comply` / `refactor files into structure` / `repo hygiene` all miss→HIT. Fix pattern = "Agent Queries Answered" section with the PM's actual phrasings. **Disambiguation:** `docs/REPO_HYGIENE_PLAN.md` (repo-wide deletion checklist, NOT in RAG, 4 days stale, scripts/ 404→576) is a DIFFERENT thing from the placement process. ⚠ supersedes M2-complete card's spec paths (now `studies/iv_weekly_substrate/specs/`). |
| [2026-08-29_sqlmesh_iv_weekly_substrate_m2_complete.md](2026-08-29_sqlmesh_iv_weekly_substrate_m2_complete.md) | SQLMesh weekly-IV substrate: M2 `iv_weekly_percentile` built + promoted — BUILD COMPLETE | M2 (FULL python, 12 cols) through all 4 gates, **no deviation**. Gate-1 dispatch `outcome=ok` (42 msgs/670s, detached nohup+poll pattern); KASA 6/6; Gate-2 certified; Gate-3 dev `m2_dev` + prod rc=0, token consumed, post-write 6/6 PASS. **OQ-1 ratified** (infeasible 2024-02-05 reconciliation KASA → monthly frozen 252d KA + Pin 6 bit-faithfulness). **OQ-2 keep-bypass** (PM: `columns=[...]` is NOT fingerprint-inert — `definition.py:1173-1175` feeds `data_hash`; M1 file untouched `3da25b26…`). **F10:** wk1/wk2 252d all-NULL in window (data not defect); monthly first-valid 2024-05-24. Standing rule honored (M1 migration error did not recur). M2 prod 3,192 rows; M1 unchanged. Evidence `verify/m2_gate3_20260829T104500Z/` + verify-run transcripts. |
| [2026-08-28_repo_cleanup_fork_and_47gb_rewrite.md](2026-08-28_repo_cleanup_fork_and_47gb_rewrite.md) | opencode_memory fork resolved + the 4.70 GB that made master unpushable | **Fork origin 2026-08-27 `bcb54104`:** a seat with cwd /data/agentic_trading resolved the memory path relative to its own cwd; bidirectional (29 cards only here, **8 only in /data** incl. the CANONICAL frozen baseline). Folded in `372055c`; /data is now a **symlink** here, which removes the cause. **Rewrite: 4.70 GB -> 531 MB.** Key reframe: the blobs were only in UNPUSHED commits, so master never needed a purge — only its commits had to stop referencing them (plain fast-forward, no --force). **Reconstructibility check (reusable):** a rewrite never touches the working tree, so the ONLY loss vector is a git-only version; across 22,104 files exactly ONE was (`research.duckdb` — **same 651,177,984 bytes**, different hash, DuckDB rewrites in place, so a size check misses it). **NEAR-MISS:** a directory-scoped removal swallowed `restore_base_tables.sql` + `manifest.json` — the recovery RECIPE — restored `cc6fdefa`; the correct scope was the one `.gitignore` already stated. **Why rules were inert:** .gitignore never applies to tracked files (research.duckdb/.external/ were ignored AND committed). **Traps:** gc reclaimed nothing because my own `stash create` snapshot pinned **5.32 GB** (re-mint post-swap = 16.5 MB, then 4.5G->**817M**); `cat-file --batch-check` without `%(rest)` silently reports WRONG totals. **Technique:** copy branch -> filter/rebase in scratch worktree -> verify -> **`git reset --mixed`** swaps the ref without touching a dirty tree. Rebase 33->6 commits (23 auto-skipped as already-applied, 4 memory-only), pushed at **0.5 MB**. |
| [2026-08-28_qwen38_reboot_restore_latency_decomposition.md](2026-08-28_qwen38_reboot_restore_latency_decomposition.md) | Qwen3.8 post-reboot restore + latency decomposition — what is tunable and what is not | **Restore = 2 commands** (`systemctl --user stop ik-llama-server.service`, then the launcher BARE — its defaults are the dedicated-gpu2 slot); healthy 45 s, KV pool reproduced at **1,504,032**. PM HOLD: 3.8 keeps gpu2 until in-flight work ends; tri return still planned so the boot unit **stays enabled**. `qwen36-coder.service` now exists + self-starts (3.8 is the only manual step). **Live /metrics, 45 reqs:** prompt 55,407, TTFT **1.47 s**, out 519, e2e 7.13 s, decode **~92 tok/s**, prefix cache **91.1%**, 0 capacity waits, spec-decode 0.848/0.715/0.599. **Latency is fully explained by output length — no knob-shaped variance left.** coder3.8 < 3.6 is ARCHITECTURAL (27B dense vs 3B-active MoE, ~9x FLOPs/tok), not tunable. **2 stale-doc corrections:** temperature IS on the wire (options channel, coder 0.7 / validate 0.8) — the launcher's TEMP note is pre-fix; validate38-ask is already `reasoningEffort: medium`, not xhigh. **Flagged not fixed:** the launcher's "TWO SEATS: DO NOT" and "pick ONE reasoning_effort" blocks were measured at a 162,415 KV pool; dedicated gpu2 is 1,504,032 (**9.3x**) — the pressure behind both warnings is gone. **opencode_memory fork RESOLVED** (origin 2026-08-27; 8 orphans folded in sha-verified, /data now a symlink to ~/oc-ask — `372055c` + agentic_trading `4f2512a9`). |
| [2026-08-28_iron_fly_v0_local_path_fix.md](2026-08-28_iron_fly_v0_local_path_fix.md) | Iron Fly V0 gate: LOCAL path blueprint fix, standalone V2 guard, e2e tests | **All 5 steps done & committed (18f0054e).** Blueprint: auth bundle written before enforce_proofs(), LOCAL path logs FAIL proofs non-blockingly. NameError: manifest loaded in generate_local_baseline() scope. Standalone V2: requires v0_authorization.json with LOCAL_BASELINE_REQUIRED verdict + fingerprint check. Tests: 6 new TestLocalPathEndToEnd + 2 updated TestV0FailurePath, 42/42 pass. |
| [2026-08-28_iron_fly_v0_baseline_gate_adjudication.md](2026-08-28_iron_fly_v0_baseline_gate_adjudication.md) | Iron Fly V0 Baseline Gate — FINAL BUILD ADJUDICATION | **Verdict: BLUEPRINT REVISION REQUIRED.** FROZEN path correct & verified (hash bindings, 0444, immutability abort, 36/36 tests). Core objective (LOCAL path) broken: (1) gate `generate_local_baseline()` NameError on `manifest` (coder-introduced, :1267/:1284) after writing local_baseline.csv; (2) gate never writes v0_authorization.json on LOCAL verdict — enforce_proofs() aborts first (blueprint ordering contradiction); (3) standalone iron_fly_v2_local_baseline.py UNGUARDED (runs RC=0 with no auth file); (4) `__main__` LOCAL branch is dead code. Root cause = blueprint, not coder. |
| [2026-08-28_iron_fly_v0_baseline_gate.md](2026-08-28_iron_fly_v0_baseline_gate.md) | Iron Fly V0 Baseline Compatibility Gate — implementation | **3 scripts + output dir:** `iron_fly_v0_baseline_gate.py` (5-axis gate), `iron_fly_v2_local_baseline.py` (local baseline generator), `iron_fly_v0_regression_tests.py` (4 regression tests). **V0 gate result:** FROZEN_BASELINE_COMPATIBLE (5/5 axes PASS). Current substrate: 352 trades (173 g5 + 179 g25), ~1,799 rows/trade (intraday), date range 2022-06-10→2026-08-21. **Regression tests:** 4/4 PASS — Test A (compatible), Test B (incompatible granularity), Test C (population mismatch), Test D (false compatibility guard). **Local baseline:** P35=0.685, P45=0.605, P50=0.560, P35→45=0.884, P35→50=0.817, P45→50=0.925, P50→60=0.817. |
| [2026-08-27_sqlmesh_iv_weekly_substrate_g1_bounce.md](2026-08-27_sqlmesh_iv_weekly_substrate_g1_bounce.md) | SQLMesh weekly-IV substrate: M1 bounced at Gate-1 (twice), root cause = workdir output-token cap | Session `ses_fbdeafe80ffe0V9oJg4sDVsmlG` (recovered from opencode.db). Building M1 `iv_weekly_state_daily` (INCREMENTAL lookback 3) + M2 `iv_weekly_percentile` (FULL, hard-gated on M1) — the iron-fly L1 conditional-entry data dependency. **Both Gate-1 builder dispatches hit `finish:length` @ 16000 out, zero artifacts.** Root cause: `/var/tmp/oc_sqlmesh_builder/opencode.json:17` pins `output:16000`, shadowing the global 32768; qwen3.8 `reasoningEffort:medium` burns the whole budget thinking before emitting. D1 (fail-closed artifact gate) + D3 (workdir sqlmesh.yml) deployed & working — bounces now return `rc=3,artifact_failure` not silent `ok`. Gate-0 passed both models; hardened prompt staged (embeds A1–A9+KAS facts). **Open:** raise cap to 32768 and/or lower reasoningEffort, then re-fire M1 Gate-1 (verify `--agent sqlmesh-builder` forwarded). |
| [2026-08-27_v0_baseline_gate.md](2026-08-27_v0_baseline_gate.md) | V0 Baseline Compatibility Gate — mandatory pre-study gate | 5-axis check (population, trade_definition, observation_granularity, outcome_construction, race_acquisition_definitions). Decision rule: all 5 PASS → frozen baseline; any FAIL → local baseline required. Scripts: v0_baseline_gate.py (reads manifest), v2_local_baseline.py, v0_regression_tests.py, generate_baseline_manifest.py. 4 regression tests (A-D) all PASS. Current verdict: FROZEN_BASELINE_COMPATIBLE (5/5). Baseline manifest: baseline_manifest.json with deterministic baseline_id (SHA256 of file hashes). Delta authority: every conditional output must carry baseline_authority, baseline_id, baseline_granularity, baseline_n. Docs: baseline_compatibility_checklist.md, research_style_guide.md, baseline_gate_demo.ipynb. |
| [2026-08-27_l1e_interaction_conclusions.md](2026-08-27_l1e_interaction_conclusions.md) | L1-E: Interaction study — ratio quartiles do ALL the discrimination | **Ratio quartiles:** Q4 (88 trades): P35→50=89.8%, P35→45=97.7%. Q1-Q3 (264 trades): P35→50=0–33%. **Interaction:** Q4 × D1_low adds +5.2pp on P35→50 (n=20, modest). Q2/Q3 cells: statistically meaningless (n_denom=2 for P35→50). **Independence:** all |r| < 0.11 — signals independent but don't improve discrimination. **V0 Gate:** 5/5 PASS, frozen baseline authoritative. **Conclusion:** ratio is a sufficient statistic. Positioning adds no meaningful incremental value. L1-A ratio Q4 gate captures 25% of trades with near-perfect metrics. |
| [2026-08-27_l1d_positioning_results.md](2026-08-27_l1d_positioning_results.md) | L1-D — Option positioning / gamma structure at Monday 11:30 | 352 trades, 10 dates excluded. **L1-D1 (OI structure):** oi_gamma_total L bin = path-quality selector (P35=-7.2pp, P35→45=+11.8pp, P35→50=+11.6pp). put_call_gamma_ratio H = path-quality (+8.8pp to 45%, +10.6pp to 50%). delta_oi_imbalance L = path-quality (+6.7pp to 45%, +7.3pp to 50%). gamma_location_imbalance L = acquisition selector (P35=+8.8pp, P50=+10.3pp). **L1-D2 (signed gamma, 56 trades):** provisional, small samples. **Cross-check:** oi_gamma_total signal partially confounded with L1-A ratio. **Verdict:** L1-A ratio remains strongest dual selector; oi_gamma_total and put_call_gamma_ratio are complementary path-quality selectors. |
| [2026-08-27_l1b_monday_realized_state_null.md](2026-08-27_l1b_monday_realized_state_null.md) | L1-B — Monday realized state: null result, no independent selector | Overnight gap, pre-11:30 direction, absolute movement, realized range — all ±0.5pp deltas, no signal. `range / friday_straddle` H bin (P35=100%) verified but is denominator-driven (correlation -0.39 with straddle; H bin mean=25 vs L bin=151). **Key insight:** sharpens L1-A — the signal isn't "high credit" or "small straddle" alone, it's `entry_credit / friday_straddle` (relative premium structure) as a strong dual selector. |
| [2026-08-27_l1_candidate_independent_path_baseline.md](2026-08-27_l1_candidate_independent_path_baseline.md) | L1 CANDIDATE v1 — independent-path baseline (absolute-value touch thresholds, 352 trades) | Computed from substrate rows with `max_pnl >= X% * abs(entry_credit)`. **Acquisition:** P(35%)=68.5%, P(45%)=60.5%, P(50%)=56.0%. **Race:** P(35→45)=80.9%, P(35→50)=74.7%, P(45→50)=90.1%, P(50→60)=80.2%. Comparison to frozen baseline: deltas 0–6.6pp (larger on conditional chains due to absolute-value threshold + smaller denominators). **Purpose:** every L1 conditional study must report deltas against these estimands. |
| [2026-08-27_opencode_temperature_options_passthrough.md](2026-08-27_opencode_temperature_options_passthrough.md) | opencode never sends `temperature` to OpenAI-compatible providers — fix is the `options` block | **Native agent `temperature` is DROPPED on the wire for `@ai-sdk/openai-compatible`; `top_p`/`top_k` survive.** vLLM then falls back to `generation_config.json` (1.0). Known upstream + unresolved: #25755 (reproduced via tcpdump on lo:8080), #5037, #2785, #5674. Present in **both 1.17.11 and 1.18.23** — upgrading does NOT fix it; v1.18.19 "removed built-in Qwen sampling defaults" is a false lead. **Three-layer proof:** config declares 0.6 -> `opencode debug config` resolves 0.6 -> **server receives 1.0**. A resolver dump is NOT evidence of what is sent; only `--enable-log-requests` / tcpdump settled it. **Fix:** `temperature` is a *recognized* field so opencode intercepts it; unrecognized fields are forwarded verbatim, and `options` is that channel (which is why `reasoningEffort` always worked). Applied at model level beside `reasoningEffort`: `coder38-ask` L351 temp **0.7**, `validate38-ask` L390 temp **0.8**; coder3.8 native field L722 0.6->0.7 (inert, kept consistent). VERIFIED on the wire against the live config. **RESOLVED SINCE:** isolation test done — agent-level AND model-level `options` each carry temperature independently (probe 0.33 / 0.44 on the wire); all 9 declaring agents now send it (`8e2b471a`), only 3 actually changed behaviour (`coder`, `qwen-coder`, `ask`: 0.6 -> 0.7), other 4 already matched their server default by coincidence; :8081 prior value upgraded inferred -> observed via `/props` (`ae2d05a9`). **⬜ TODO:** :8080 (`ask`/`build`/`qwen`) prior value is STILL UNVERIFIED — the 397B was down, so "was running 0.6" rests on the launcher flag, not behaviour. When it is up: `curl -s :8080/props` and read `default_generation_settings.params.temperature`. If it is not 0.6, the in-file comments on those three agents are WRONG. **Open:** precedence when both `options` levels disagree untested; :8081 not wire-verified (logs to a socket). |
| [2026-08-27_l1_study_hierarchy_banked.md](2026-08-27_l1_study_hierarchy_banked.md) | L1 study hierarchy banked — signal ranking + population governance | **Population is a GOVERNED artifact, not inferred from the current substrate:** frozen baseline 352 trades (173 g5 / 179 g25), 173 paired weeks, 2022-06→2026-08; substrate drifted 352→362 (10 new dates), remediated by an explicit 10-date exclusion — every subsequent script must state its population definition and exclusion list. **L1-A `entry_credit / friday_straddle` is the DOMINANT dual selector** (Q4: P35=100%, P35→45=93.2%, P35→50=89.8%, P45→50=95.5%; +38.7pp acquisition AND +10.6pp path quality). **L1-D positioning/gamma is COMPLEMENTARY, not a substitute** (oi_gamma_total low, put_call_gamma_ratio high = path-quality; gamma_location_imbalance low = acquisition). **Nulls:** L1-B Monday realized state (±0.5pp), L1-C volatility regime (±0.1pp). Next = L1-E interaction. |
| [2026-08-27_iron_fly_100w_economic_baseline.md](2026-08-27_iron_fly_100w_economic_baseline.md) | CANONICAL BASELINE — frozen population/estimand definitions + benchmark probabilities for conditional studies | Thin pointer to `outputs/iron_fly_100w_baseline_economic/`. Population: 352 trades, 173 paired, 2022-06→2026-08, economic fields, substrate v2. **Acquisition:** P(35%)=68.8%, P(45%)=61.3%, P(50%)=56.1%. **Race:** P(35→45)=82.6%, P(35→50)=77.3%, P(45→50)=94.1%, P(50→60)=86.8%. **Hold-Friday:** win=46.3%, mean=-0.84. **Self-check:** 8/8 PASS. **Purpose:** every conditional study must report deltas against these frozen baselines. Immutable definitions: eligible week, body-grid pooling, target definition, race barrier, economic marking, entry time, sample period. |
| [2026-08-27_iron_fly_100w_unconditional_baseline_economic.md](2026-08-27_iron_fly_100w_unconditional_baseline_economic.md) | Iron fly 100W consolidated unconditional baseline from verified economic-field outputs | Script `iron_fly_100w_baseline_economic.py` produces 7 files in `outputs/iron_fly_100w_baseline_economic/`. All 8 self-checks PASS. **The paradox:** 76% reach 35% credit capture, 62% reach 45%, 56% reach 50%. Once entered, continuation is strong (35→50 wins 77%, 45→50 wins 94%). Yet hold-to-Friday win rate is only 46.3%. **Resolution:** giveback. After touching 35%, P(return≤0)=76.9%. Wings cost 19.0 on average but overlay at MFE is -9.5 (drag). **PM conclusion:** strategy viable only when wing cost < expected upside continuation value. **Next:** conditional entry L1. |
| [2026-08-27_iron_fly_selfcheck_economic_decomposition.md](2026-08-27_iron_fly_selfcheck_economic_decomposition.md) | Iron fly 100W studies: update S3/S4 wing overlay + S2 mark diagnostic to verify economic decomposition | **S3 (wing overlay):** changed from provider-mid delta identity to economic leg mark decomposition: `fly_value_economic == pw_econ + cw_econ - pb_econ - cb_econ` (substrate line 428). **S4 (wing overlay):** unchanged (already verified overlay recomposition). **S2 (mark diagnostic):** replaced mid-based fly_identity check (`fly_value_economic == -close_cost`) with economic leg mark decomposition check. All 8 studies now PASS self-checks: Study 1 (V7 economic anchor), Study 2 (continuation), Study 3 (race), Study 4 (V2 overlay), Study 5 (S3/S4 economic), Study 6 (S2 economic), Study 7 (raw rederive), Study 8 (spot check). |
| [2026-08-27_iron_fly_economic_field_migration.md](2026-08-27_iron_fly_economic_field_migration.md) | Iron-fly 8 consumer scripts: TABLE→stable view + mid→economic field rename | All 8 scripts updated: `__1230283500` hash→`warehouse.iron_fly_weekly_substrate_v2`; `fly_value_mid`→`fly_value_economic`, `fly_value_bidask`→`fly_value_economic`, `entry_debit_mid`→`entry_debit_economic` in SQL + Python dict access + COL dicts + IS NOT NULL guards + sign convention comments. All 8 pass `py_compile`; zero old hash refs; zero mid-field dict access remaining. |
| [2026-08-27_iron_fly_v2_econ_promote_plan.md](2026-08-27_iron_fly_v2_econ_promote_plan.md) | Iron fly v2 economic substrate — normal `plan prod` promotion, pre-token verification + categorization de-risk | Patched `iron_fly_weekly_substrate_v2` (economic leg marks + `entry_debit_economic` on path basis, Monday ≤15:59 cap) committed `c2da4c04`+`951a526c`; 22:35 restate was a NO-OP (restate re-backfills STATE model, not working tree) → PM switched to **normal** `plan prod --auto-apply --no-prompts` (no `--restate-model`). **Categorization risk RESOLVED (corrects earlier "mode OFF/UNKNOWN" error):** auto-cat defaults to `sql: FULL` (not OFF); `is_breaking_change`→None→**BREAKING**; definitive read-only `plan_builder("prod").build()` against real DB: `plan.uncategorized`=0 (so `--no-prompts` will NOT fail), old version `1230283500`→**new `1103730983`**, only substrate modified, new physical version + view promotion (the PM path). The `diff` "(Unknown)" label is a ContextDiff display red herring, not the plan-time gate. Gates harness built+verified (`scripts/iron_fly_v2_econ_gates.py`, selftest ALL_PASS, negative control correct). **Recurring RW writer DISABLED (2026-08-27):** `daily_eod_build.service` patched with `--skip-shadow` (ExecStart verified live via `systemctl show`), `daemon-reload + restart timer` done. Steps 1–9 (parquet rebuilds) still run; step 10 (warehouse shadow/RW to duckdb) skipped. 00:20:50 mystery write still unattributed (no journal entry) but no longer a blocker. Token argv hash `bcbc7f0d…`; next = mint token → execute plan (~7–10 min backfill) → Gates 1–9 → Phases 1–5 (8 consumers). |
| [2026-08-26_iron_fly_mark_quality_valuation.md](2026-08-26_iron_fly_mark_quality_valuation.md) | Iron-fly mark-quality convention + PM-confirmed wings-only nonsensical-tick transform (overlay) | Component valuation, NOT arb-free package quote. Usable market→provider mark (kept even past 100-pt cap); unusable/nonsensical owned wing→max(intrinsic,0) (OTM→0, ITM→intrinsic). Trigger (heuristic, not no-arb): provider_mid > intrinsic+50 AND > 50; WINGS ONLY, bodies unchanged. NO clamp to [-100,0], no drop, no substrate/model change (diagnostic proved construction clean: strike_rel_error=0). Implemented `scripts/iron_fly_wing_mark_overlay_100w.py` + outputs in `outputs/iron_fly_wing_mark_overlay_100w/`: 7,102 wing transforms, 5,789 marks / 34 trades revalued (median Δ −58.5, min −4852), 627,936 unchanged, bodies untouched (S1–S4 PASS, independent verify). Residual: 1,843 marks still |fly|>150 after transform, 1,540 driven by untransformed body-leg glitches (out of wings-only scope, documented). PM example CORRECTED: actual upper_wing=5060 → call wing ITM by 82.75, transform→82.75 not 0; fly_adj −224.60 verified. Frozen L0/L0.5/L0.6 on canonical substrate unaffected (<0.008 on core races). Committed ff7640b2 (diagnostic) + b5006972 (transform overlay). |
| [2026-08-26_iron_fly_100w_race_frozen_geometry.md](2026-08-26_iron_fly_100w_race_frozen_geometry.md) | 100W iron fly L0.6: upside-vs-giveback first-passage RACE, frozen exit geometry (PM-corrected) | Commit `66cd81c8` (script race_matrix.csv golden_path.json self_check + 2 verify deposits; parquet excluded). After first touch A, which resolves first: upside B or giveback D (unresolved = own bucket, ≤3.3%). Combined core: 35→45 before 25 = 71%; 35→50 before 25 = 64%; 40→50 before 30 = 70%; 45→50 before 35 = 81%; 45→60 before 35 = 63%; 50→60 before 40 = 73%; 35→60 before 25 = 50.6%. **PM-corrected frozen interpretation: geometry is STATE-DEPENDENT; exiting 35–50% just because "the winner might disappear" is not supported (10-pt races upside-first 64–81%); 60% is the first meaningful continuation FRONTIER, not the exit boundary — only the +15pt extension from an early 35% state is ~indifferent.** Verified: 5 self-checks PASS + independent raw-row recompute 30/30 core + full 91-row combined matrix EXACT. ~53–58% of upside resolutions land at time-0 (touch mark already ≥ B); decision-relevant medians (if later) 0.4–4.1 h, 17.9 h for "50 before 0". Re-scoped next: conditionals must improve BOTH acquisition AND continuation. |
| [2026-08-26_iron_fly_100w_continuation.md](2026-08-26_iron_fly_100w_continuation.md) | 100W iron fly L0.5: continuation P(reach B|reached A) + give-back tail, frozen | Commit `8a5d9724` (script + continuation_matrix.csv giveback_table.csv base_rates.csv self_check + 2 verify deposits; parquet excluded). P(reach B\|reached A)=P(MFE≥B)/P(MFE≥A); give-back anchored at touched level A. Continuation stays 68–94% through 35–45 (only far 75% crosses under 50%); but P(return≤0) after touching 35% = 60.8%, P(≥20pp give-back) = 74.7% — highly oscillatory. Spec correction: my give-back inequality was backwards (true order gb5≥gb10≥gb20≥≤0); numbers were correct. Verified 159/159 cells + all rows EXACT by independent recompute. |
| [2026-08-26_iron_fly_100w_pm_ruling_conditional_entry.md](2026-08-26_iron_fly_100w_pm_ruling_conditional_entry.md) | PM ruling on 100W L0 iron fly path study: next step is conditional entry, not execution | L0 frozen (c64a1d18+6d82b3bc+4c663e1b). PM: gross intraweek capture established, net executable expectancy NOT (bid/ask is intentionally harsh top-of-book bound, not live fill quality). Priority = identify entry-time conditions raising frequency+depth of the 35–45% favorable excursion (hit ~0.70@35%, MFE median ~57% of credit). If strong subgroup emerges → execution becomes separate study, may justify depth/complex-order data. Non-goals: no stop-loss, no 75W/120W, no execution modeling yet. |
| [2026-08-26_sqlmesh_rag_refill_clobber_seed.md](2026-08-26_sqlmesh_rag_refill_clobber_seed.md) | sqlmesh_reference RAG: scan-stage clobbered reviewed staged file, re-filled 13 cards, 3 opus reviews, seeded both roots live | **211 chunks LIVE** on both small + full roots; 3 opus reviews caught 8 total errors (model count, model lists, context.table() example, resolve_table, contradictory our_usage, placeholder); YAML source-of-truth still stale — next scan-stage after model change will clobber again |
| [2026-08-25_iron_fly_100w_path_study.md](2026-08-25_iron_fly_100w_path_study.md) | Iron-fly 100W path study over v2 substrate (Mon 11:30 short iron fly, 100W, body 5 vs 25) | `scripts/iron_fly_path_study_100w.py` + 5 outputs in `outputs/iron_fly_path_study_100w/`; sign convention verified (pnl = fly_value_mid - entry_debit_mid); **caught _percentile sorted-input bug** (all percentiles wrong, means right) — fixed + independently re-verified; 173/179 enterable trades; hold-to-expiry worst (win 0.47/0.45, mean −2.5/−2.0); mid take-profit: ~50% capture = mean-PnL optimum (+7.1), 15–25% = frequency optimum (0.86/0.76 hit); **executable-side collapses the edge** (mean PnL ≤0 at 10–25%, crosses 0 ~30%); body 5 vs 25 second-order at 100W; committed (frozen L0) c64a1d18+6d82b3bc+4c663e1b; next = conditional-entry L1 (see 2026-08-26 ruling card) |
| [2026-08-25_sqlmesh_rag_cards_13_filled.md](2026-08-25_sqlmesh_rag_cards_13_filled.md) | 13 sqlmesh_reference RAG cards: filled all 44 N/A sections, double-reviewed (qwen-coder + claude-opus-4-7 headless), all fixes landed | **211-chunk** staged file at `/data/parquet/verifier_kb/staged/sqlmesh_reference.jsonl`; 131-claim fact-check 106✓/3✗fixed/22 framework; caught off-by-one body rotation in 2 cards + invented dev-table naming + misleading `df=context.table()`. Verdicts: 4 approve / 4 notes(fixed) / 4 rework(fixed). **Live service reads `verifier_kb_small` (stale 97-chunk 08-17 copy) — refresh/stage is a PM-gated GPU step, new content NOT live.** Pilot cards (agent_activation, agent_guidance) still N/A. |
| [2026-08-25_tricard_qwen38_rag_coresidency.md](2026-08-25_tricard_qwen38_rag_coresidency.md) | Does the 3.8 coder fit co-resident with the tri-card 397B + small RAG? | **YES — it is what `launch_qwen38_27b_fp8.sh`'s bare defaults are sized for.** Per-card: gpu0 tri 53,884 + 3.8 @ UTIL=0.40 39,155 -> ~5,930 free; gpu1 tri ~81,627 + small RAG 6,038 -> ~9,946 free; gpu2 tri alone -> ~18,305 free (~34 GiB idle box-wide). Composes because 3.8 and RAG never share a card — gpu0 = the 2026-08-23 live numbers (tri WAS resident), gpu1 = the 2026-08-20 trio measurement. WHICH RAG is the variable: small root (6 GiB) fits gpu1, full-root 8B (15.3 GiB) does not and must go to gpu2 (~2.6 GiB left). Cost to the seat: relaunch off gpu2, ctx 262144->131072, KV ~53 GiB -> 5.87 GiB (162,415 tok, 1.24x), ONE seat only, fp8 KV load-bearing. 3.6 must be off (40,072 MiB vs 43,579 free — gpu0 hosts exactly one). Blocker today: ComfyUI holds 35,868 MiB on gpu1 where the tri needs ~81.6 GiB; tri is DOWN. Flagged not fixed: note §6 says mnbt 4096/never-8192, committed launcher carries 16384. |
| [2026-08-25_iron_fly_substrate_v2_sixcell.md](2026-08-25_iron_fly_substrate_v2_sixcell.md) | iron_fly_weekly_substrate_v2: 6-cell (grid 5/25 × wing 75/100/120) parameterized build | **FULLY VERIFIED**: restatement succeeded (1,939,361 rows / 181 weeks / 6 cells / 2022-06-06→2026-08-21); reconciliation 0/0 on 19 cols across 5 deterministic weeks (independent parquet recompute; bug was in MY verifier's put-wing mid term, not the model); adapted v2 KASA 14/14 PASS; 8 structural checks PASS; transition COMPLETE: v2 committed 14c7e148, v1 reference parquet exported (sha256 recorded), v1 model file retired 167e9e0a, prod plan to drop v1 applied via guarded executor (review-first, zero physical changes, diff clean), RAG card iron_fly_vs_fly_disambiguation + 3 dependent clauses repointed to v2 (live-served rank#1, committed 98fc1580). **TRANSITION COMPLETE end-to-end; nothing open.** |
| [2026-08-25_iron_fly_substrate_rag_disambiguation.md](2026-08-25_iron_fly_substrate_rag_disambiguation.md) | Iron fly substrate built WRONG (body nearest-25/wings±25) — root cause: RAG "fly" cards all mean 0DTE put-butterfly | spec=body nearest-5, wings ±100 (200 total), 4-leg, Mon11:30/Fri15:59, DTE4; fix committed f45b0c28 = new iron_fly_vs_fly_disambiguation card + SCOPE prefixes on 7 butterfly cards + downgraded "only canonical house interpretation"; seeded both roots, verify-run 1/1; 2 reviews (qwen-coder EXPLORE + claude-opus-4-7) approved; open = rebuild substrate to spec (substrate-only, no PnL), task spec must carry the 4 params explicitly |
| [2026-08-25_qwen36_bounded_thinking.md](2026-08-25_qwen36_bounded_thinking.md) | qwen3.6 coder bounded thinking: --think-budget N, and how not to probe it | min_p is a truncation filter (narrows, never adds reasoning) and reasoning was OFF at the server; real lever is llama.cpp --reasoning-budget N>0 + close message; log markers activated/exhausted/natural-end are the diagnostic; 8 exhaust + 2 natural, every capped answer correct incl. a truncated 5-leg P&L chain; keep 256; 2 of 4 probes defective (recall-contaminated + ambiguous wording); the one real error was a fencepost with reasoning intact = verification gap not budget gap; agentic loops still untested |
| [2026-08-25_agents_md_double_load.md](2026-08-25_agents_md_double_load.md) | AGENTS.md double-load: symlinking a second root injects the doctrine twice | opencode merges TWO instruction routes (dir-search up to project root + global `instructions` list) and dedupes by path TEXT not realpath, so a symlink = 2 paths = 2 copies; oc-ask was 28789 -> 19192 chars; oc-seat verified clean both launch modes; raw-packet probe recipe; fixed in oc-ask@9906220 + market_data@623a81e4 |
| [2026-08-25_gpu_load_query.md](2026-08-25_gpu_load_query.md) | Query the GPU directly: what is loaded on each card | nvidia-smi --query-compute-apps / --query-gpu; driver knows PID+bytes only, model identity is a cmdline join; vLLM renames to VLLM::EngineCore (walk to parent); pgrep -f vllm self-matches |
| [2026-08-24_sqlmesh_agent_deploy_walkthrough.md](2026-08-24_sqlmesh_agent_deploy_walkthrough.md) | SQLMesh agent runtime: end-to-end deploy walkthrough + oc_server.sh exec-bit fix | Gate 0->3 command sequence (spec_intake_check -> dispatch_sqlmesh_builder_gated -> certification_gate -> guarded sqlmesh_executor -> post_write_verify); live workdir is /var/tmp/oc_sqlmesh_builder (UNDERSCORE; sealed card says hyphen); executor gotchas (`--` separator, one-shot PM token, --auto-apply + --no-prompts, timeout=exit 3 no-retry); iron_fly candidate blocked |
| [2026-08-24_qwen38_validator_detune.md](2026-08-24_qwen38_validator_detune.md) | Qwen3.8 validator detune: temp 0.8 + reasoning_effort medium, all 4 surfaces, durable via registry | two-lane config map (global opencode.jsonc vs staged registry flow); stager now emits reasoningEffort (fail-closed); activation = restart THEN restage; 5 files uncommitted; lockstep drift check deferred by PM |
| [2026-08-24_sqlmesh_daily_cycle_repair.md](2026-08-24_sqlmesh_daily_cycle_repair.md) | SQLMesh directive + INCIDENT: REMOVE deleted PROD db (symlink); restored + advanced + verified + committed | symlink-before-rm rule; backup cron PATH fixed (3 days false "corrupted"); step-9 hardened; exec-bit fix; 2 commits pushed (7 files); full directive verification with PROOF |
| [2026-08-24_qwen36_262k_restart.md](2026-08-24_qwen36_262k_restart.md) | qwen3.6 gpu1 restart: 262k ctx + q8_0 KV; full action plan preserved | config edits (L166/L945), setsid launch fix, rollback recipe, deltanet-MoE watch item, serve-restart DONE via -y |
| [2026-08-24_gpu2_slot_inspection.md](2026-08-24_gpu2_slot_inspection.md) | GPU2 ComfyUI slot: what's running + vantage proof | PID 1190318 Wan I2V live, 81-frame links ~52s, netns/Seccomp host proof |
| [2026-08-24_opencode_memory_system.md](2026-08-24_opencode_memory_system.md) | Session memory layer design + build | opencode_memory/ + AGENTS.md trigger phrases, transcript locations, RAG gap |
| [2026-08-24_sandbox_vantage_provenance.md](2026-08-24_sandbox_vantage_provenance.md) | Sandboxed observability section: provenance audit + swap_model.py fix | provenance per block (user vs assistant), live netns probes, adjudication: stay-all, commit 5c2d91fc |
| [2026-08-24_wan22_i2v_chain.md](2026-08-24_wan22_i2v_chain.md) | Wan2.2 I2V ComfyUI 3-link chain, mid-chain waypoint | workflow design, model inventory, frame extraction, session decisions |
